name: Mastodon growth loop

# Cada día a 10:00 UTC (=12:00 CEST) — 30 min después del bluesky-growth
# para escalonar carga y evitar rate-limits simultáneos.
# Ejecuta el growth loop (follow + favourite + reblog por hashtag ES).
# 2x/día (21/09: estrategia engagement — Mastodon no crecía; +reblogs +hashtags).

on:
  schedule:
    - cron: "18 10 * * *"   # mañana
    - cron: "18 18 * * *"   # tarde
  workflow_dispatch: {}

concurrency:
  group: mastodon-growth
  cancel-in-progress: false

permissions:
  contents: write  # reusable job commite stats_history

jobs:
  growth:
    uses: ./.github/workflows/_videogen-job.yml
    with:
      command: mastodon-growth
    secrets: inherit
