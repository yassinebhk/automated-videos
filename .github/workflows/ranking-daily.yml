name: Ranking daily (TopRanking ES) — PAUSADO 15/09/26

# ⚠️ CRON DESACTIVADO 15/09/26 — user reportó datos FALSOS (Cristiano
# 9º en 2024, ejemplo hipotético que Gemini inventó). Viola política
# veracidad-obligatoria. Pipeline sigue disponible via workflow_dispatch
# manual pero no publica automático hasta que:
#   1. Sustituyamos generación LLM por datasets REALES bundled
#      (Wikipedia/World Bank/UN/Forbes JSON estáticos)
#   2. O el topic pool se limite a rankings donde Gemini SÍ tiene
#      datos fiables (top 10 estáticos históricos consolidados)

on:
  # 15/09/26 REACTIVADO tras cablear datasets REALES bundled
  # (output/ranking_datasets/). Ver ranking-datasets-refresh.yml.
  schedule:
    - cron: "33 15 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  ranking:
    uses: ./.github/workflows/_videogen-job.yml
    with:
      command: ranking-once
    secrets: inherit
